# 이 파일을 .env.local 로 복사한 뒤 키를 채우세요. (.env.local 은 git에 올라가지 않습니다)
# 주의: VITE_ 변수는 빌드 시 클라이언트 번들에 인라인됩니다.
#       내 컴퓨터에서만 쓰는 로컬 용도로만 사용하고, 공개 배포는 하지 마세요.
VITE_ANTHROPIC_API_KEY=sk-ant-...

# 선택: 모델 변경(기본 claude-sonnet-4-6). 더 높은 품질이 필요하면 아래 주석 해제.
# VITE_ANTHROPIC_MODEL=claude-opus-4-8
